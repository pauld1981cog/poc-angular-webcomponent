import { TurboPhonePipe } from './turbo-phone.pipe';

describe('TurboPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new TurboPhonePipe();
    expect(pipe).toBeTruthy();
  });
});

import { TurboCurrencyPipe } from './turbo-currency.pipe';

describe('TurboCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new TurboCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
